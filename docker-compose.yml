version: "2"
services:
    service-a:
        build: ./service-a
        image: registry.marathon.mesos:5000/sample-app/service-a
        environment:
            - APPINSIGHTS_INSTRUMENTATIONKEY
        ports:
            - "8080:80"
        links:
            - service-b
    
    service-b:
        build: ./service-b
        image: registry.marathon.mesos:5000/sample-app/service-b
        environment:
            - APPINSIGHTS_INSTRUMENTATIONKEY
        expose:
            - 80
